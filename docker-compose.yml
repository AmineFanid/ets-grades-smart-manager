services:
  db:
    image: postgres:15
    container_name: ets-manager-db
    environment:
      POSTGRES_USER: amine
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: manager_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: